# 产品与需求变更规则

本文档约定：**凡是本产品（英语口语真人匹配练习平台）的需求、规则或业务结论发生变化，必须先改文档，再（或同时）改代码。**  
避免「口头改了需求、仓库里的说明仍是旧版」导致实现与产品不一致。

---

## 1. 文档分工

| 文档 | 路径 | 用途 |
|------|------|------|
| **产品描述** | `docs/产品描述.md` | 功能范围、用户旅程、配对与信用分规则、兜底策略、通知与主题等**正式产品说明** |
| **架构与开发规范** | `ARCHITECTURE.md` | 技术栈、目录、数据库、API、工程侧硬约束（与产品描述对齐） |
| **用户体验框架** | `docs/用户体验框架.md` | 页面地图、各页功能、**MVP 必做**与**当前代码已有/部分/未做**对照，供设计与排期 |
| **产品功能与体验规格（AI 前端用）** | `docs/产品功能与体验规格-AI前端用.md` | 合并版：旅程、规则、页面、接口、前端约束，供交给外部 AI 做前端时作单一输入 |
| **本变更规则** | `docs/产品与需求变更规则.md` | 何时改文档、改哪些文档、**变更记录**（摘要留痕） |

新增独立说明（例如 API 约定、数据库设计）时，在下方「变更记录」里记一笔，并在本节表格中补充一行。

---

## 2. 什么情况必须更新文档

在以下任一情况发生时，**必须**更新 `docs/产品描述.md`（或新建专项文档并在本文件记录路径），并在**第 4 节变更记录**追加一条：

- 功能增删或流程变化（登录、预约、配对、房间、信用分、通知等）
- 业务规则变化（例如配对约束、加减分、阈值、兜底策略、文案策略）
- 目标用户或范围变化（如地域、MVP 边界）
- 与实现强相关的**产品决策**（例如虚拟语伴是否做、优先预约权如何定义）

**仅修 UI 样式、重构代码而不改行为**时，不必改产品描述，除非交互改变了用户可感知流程。

---

## 3. 推荐工作流

1. **先改** `docs/产品描述.md`：把新结论写进对应章节，删除或标注废止旧表述。  
2. **再改** 本文件第 4 节：用一行或一小段说明**改了什么、日期**。  
3. **最后** 再改代码 / 前端页面，使行为与文档一致。  
4. 若在 Cursor 中与 AI 协作：说明「已按 `docs/产品描述.md` 第 × 节为准」，避免模型沿用旧对话记忆。

若某次变更只涉及「变更流程本身」（例如要多维护一份 API 文档），请在本文件 **第 1 节表格** 或 **第 2 节** 中同步修改说明。

---

## 4. 变更记录

| 日期 | 摘要 |
|------|------|
| 2026-04-04 | 初始化：确立产品描述与变更规则分工；后续变更请在此追加行。 |
| 2026-04-04 | 预约：开场前 **1 小时**停止新预约；配对后须在个人中心/通知等展示**搭档昵称与水平**；新增根目录 `ARCHITECTURE.md`、`db/schema.sql`、`.cursorrules`。 |
| 2026-04-04 | 接入声网：`backend` 提供 `POST /api/agora/rtc-token`（`agora-token`）；`backend/public/room.html` 使用 Web SDK **4.23.2** + `room-agora.js`（Token 入会、麦克风、可选摄像头）。 |
| 2026-04-04 | 新增 `docs/对抗式任务玩法与界面.md`：手动完成 + 对方确认/超时、收银员/客户子任务表、双轮互换、非模态确认与折叠任务单；`产品描述` 5.4 节增加引用。 |
| 2026-04-04 | `room.html` 对齐任务确认视觉稿：深色顶栏任务卡 + `#2563EB`「完成」+ 底栏通话控制；`room-tasks.js` 演示确认条与超时。 |
| 2026-04-11 | `产品描述` 新增第 12 节（数据隔离原则）、第 13 节（实现状态表）；`ARCHITECTURE` 新增第 11 节（与代码对照）；标明预约截止、配对批处理、通知、信用结算等未实现项；说明与「AI 全量档期 / 实时队列」类方案的边界。 |
| 2026-04-11 | **场次补全**：`initDb` 增加 `ensureWeekendTimeslots`，每次启动为活跃主题写入未来合规周末 20:00 场次（去重），避免旧库非周末数据被 API 滤空后长期「无可预约」；`weekendSlotRules` 仅对周末日期去重、`toShanghaiNaiveSqlRange` 固定 `20:00`/`21:00` 写入 PG；`产品描述` 场次日历、`ARCHITECTURE` 启动约定、`产品功能与体验规格-AI前端用` 同步。 |
| 2026-04-12 | 新增 `docs/用户体验框架.md`：文档在研发流程中的用途、页面地图、分页面功能表及 **MVP 必做 / 当前代码** 标记；变更规则第 1 节表格登记该路径；`产品描述` 第 14 节状态行增加对该文档的交叉引用；`ARCHITECTURE` 目录结构列出该文件。 |
| 2026-04-13 | **删除** `docs/对抗式任务玩法与界面.md`；`产品描述` 5.4 节与实现状态表去除对该文档的依赖，改为可选迭代表述；**新增** `docs/产品功能与体验规格-AI前端用.md`（全量合并说明供前端 AI）；`用户体验框架` 房间扩展行去掉对已删文档的引用；变更规则第 1 节表格移除对抗式一行并登记新文件。 |
| 2026-04-14 | **配对策略变更**：由「开场前 5 分钟批处理」改为**同主题+场次+水平可配则尽早配对并立即展示/通知**；**一方取消**时通知另一方并**重配**；**开场前 60 分钟**停止新预约与**首次配对**，该时段内用户应已知搭档，**取消触发的重配**除外；**停配时刻**做全员可配性检查并触发原第 7 节兜底；**开场到点**对队友未上线者**落单互配**并通知。已同步 `产品描述` 5.1～5.3、7、9～10 节，`ARCHITECTURE` 5.4、6.2、6.5、11 节，`用户体验框架`，`产品功能与体验规格-AI前端用`。 |
| 2026-04-15 | **场次日历**：MVP 规定每主题仅 **北京时间周六、日 20:00** 可约；`GET /api/timeslots` 过滤；空库种子 `seedDemoIfEmpty` 按该规则生成；预约页卡片展示 **MM-DD HH:mm** 与 **星期**；`产品描述` 5.1 增补「场次日历」；`ARCHITECTURE` 接口说明更新。 |
| 2026-04-16 | **修复**：Railway 等 **Node 为 UTC** 时 naive `timestamp` 经 `node-pg` 误判导致 `GET /api/timeslots` 为空（`to_char` / `toDateForShanghaiRule` / 前端 `+08` 解析）。**预约页**：预习下载为 **`.docx`**（`docx` 包服务端生成，`GET /api/preview-material/docx`）；文案「**下载预习资料**」；正文与 `backend/data/previewMaterials.js` 与前端预览 Markdown 双处需同步。 |
| 2026-04-17 | **周主题轮换**：每周日 **19:00（北京时间）** 起开放**下一自然周**的**三个**主题；每主题仅一周，周日最后一场结束后轮换；`themes` 增周字段与轮换池 `themeRotationPool.js`；`GET /api/themes`、`/api/themes/by-id`；预约 `theme_id`；`timeslots` 仅 `theme_id`；首页动态卡片；`initDb` + 定时 `runWeeklyThemeMaintenance`；预习 docx 读 `themes.preview_markdown`。已同步 `产品描述` 5.1、8.1、`ARCHITECTURE`、`产品功能与体验规格-AI前端用`、`README`、`用户体验框架`。 |
| 2026-04-17 | **热修**：`schema.postgres.sql` 不再在 `runSqlFile` 阶段创建 `idx_themes_week_slot`（旧库无 `shanghai_week_monday` 列时会导致启动失败）；索引仅在 `migrateWeeklyThemesColumns` 于 `ALTER` 之后创建。 |
| 2026-04-11 | **周主题活跃周**：周日 19:00～21:00（上海）与「本周」周期尾重叠时，`getActiveThemeWeekMondayNow` 取**候选周一最大**的一周，使首页/预约展示下一周六日场次；`产品描述` 5.1、`ARCHITECTURE` 5.2/6.6、`产品功能与体验规格-AI前端用` 同步；预约页 `formatSlotTime` 用 `formatToParts` 保证 **MM-DD HH:mm** 分钟两位。 |
| 2026-04-11 | **取消预约**：实现 `DELETE /api/cancel-booking/:id`（`cancelBooking.js`：开场前可取消、删 `bookings` 行并 `booked_count-1`、删相关 `pairs`）；`appointments-live.js` 取消入口与二次确认；未配对时「进入房间」置灰；`ARCHITECTURE` 接口说明、`用户体验框架` 4.4、`产品功能与体验规格-AI前端用` 同步。 |
| 2026-04-11 | **开发配对接口**：非生产或 `ENABLE_DEV_PAIRING=1` 时挂载 `POST /api/dev/pair-timeslot`（`devPairTimeslot.js` + `levelCompatibility.js`），便于两名已预约用户一键写入 `pairs` 测进房；`ARCHITECTURE` 5.2、`产品描述` 第 13 节实现状态、`产品功能与体验规格-AI前端用` 同步。 |
| 2026-04-11 | **预约即可进房 + 开场配对**：预约后进 `room.html?timeslot_id=`，`POST /api/agora/rtc-token-booking` 下发等待大厅 `engw{timeslotId}`；`runAutoPairingScan` 每分钟在「已开始未结束」场次贪心写 `pairs`；`room-agora.js` 轮询切 1v1 频道。产品 5.1～5.4、5.3 节改为**开场到点后首轮配对**；`ARCHITECTURE` 6.2/6.4/6.5、README、`产品功能与体验规格`、`用户体验框架` 同步。 |
| 2026-04-11 | **房间等待 UI**：`rtc-token-booking` 增加 `startTime`/`endTime`；练习间顶区「练习间已就绪…」、开场到点后「已到练习时间…」按钮与匹配提示浮层（`room.html` + `room-agora.js` + `styles.css`）；`ARCHITECTURE` 接口说明、`产品功能与体验规格-AI前端用`、变更记录同步。 |
| 2026-04-11 | **开场前不连声网**：`timeslot_id` 且 `rtcMode=waiting` 且当前早于 `startTime` 时不再 `createClient/join`；到点后 `attemptFirstTimeslotJoin` 再入等待大厅并启动频道轮询；`产品描述` 5.4 同步。 |
| 2026-04-11 | **房间 TASKS UI**：`room.html` 任务区为「TASKS (n/m)+刷新+模拟请求」、每条任务 **CLAIM** + 可折叠 **USEFUL SENTENCES**；`room-practice-tasks.js` + `styles.css`；`room-tasks.js` 改为 `#practiceTaskList` 事件委托并与 CLAIM 文案对齐；`产品描述` 8.2、`产品功能与体验规格-AI前端用` 5.4、`ARCHITECTURE` 房间行同步。 |
| 2026-04-11 | **规划入档**：**沙箱实验室**（`产品描述` 5.6、`ARCHITECTURE` 6.7、`ENABLE_SANDBOX_LAB`、专用主题/场次、免 60 分钟停约、`dev-lab.html` 与可选 `sandbox-slot` 接口）；**豆包 LLM**（`产品描述` 8.3、`ARCHITECTURE` 6.8、`DOUBAO_API_KEY`、服务端 `doubaoClient`、落库约定）；同步 `产品功能与体验规格-AI前端用`、`用户体验框架`、`产品描述` 第 13 节表、`README`。 |
| 2026-04-11 | **沙箱实验室落地**：`themes.is_sandbox` + `migrateThemesSandboxFlag`；`services/sandboxLab.js`、`initDb`/`ensureSandboxLab`；`GET/POST /api/dev/sandbox-lab`、`sandbox-slot/refresh`；`timeslots`/`bookings`/`agora`/`booking-flow`/`room-agora` 例外；`dev-lab.html`、首页入口；`db/schema.postgres.sql`；`ARCHITECTURE` 6.7/5.2/11、`产品描述` 第 13 节、`用户体验框架` 4.7、`产品功能与体验规格` 接口表、`README` 同步。 |
| 2026-04-11 | **文档**：`ARCHITECTURE` 新增 **8.1 线上验收沙箱**（`ENABLE_SANDBOX_LAB=1` 必填、`ENABLE_DEV_PAIRING=1` 可选）；`产品描述` 5.6 与第 13 节表、`README`、`.env.example` 同步线上变量说明。 |
| 2026-04-11 | **LLM 主题 enrichment**：OpenAI 兼容 Chat Completions（豆包方舟等）；`themes` 增 `room_tasks_json`、`llm_generated_at`、`llm_prompt_version`；`initDb`/周维护调用 `tryEnrichThemesWithLlm`；`rtc-token-booking`/`themes/by-id` 返回 `roomTasks`；房间 `__applyRoomTasksFromApi`。已同步 `产品描述` 8.3 与第 13 节、`ARCHITECTURE` 6.8 与第 11 节、`产品功能与体验规格-AI前端用`、`db`/`backend` `schema.postgres.sql`、`backend/.env.example`。 |
| 2026-04-11 | **调试**：`POST /api/dev/theme-llm-rerun`（与 `pair-timeslot` 同开关）支持对指定 `theme_id` 清空 LLM 字段并立即再生成；`产品描述` 8.3、`ARCHITECTURE` 5.2、`README` 同步。 |
| 2026-04-14 | **管理员手动配对（最小可用）**：新增 `ADMIN_EMAILS` 白名单与 `/api/admin/*` 接口（查看 timeslots / bookings / pairs、手动 pair/unpair）；新增 `public/admin.html` 简版后台页。已同步 `ARCHITECTURE` 与 `backend/.env.example`。 |
| 2026-04-14 | **房间角色区与互换**：顶区改为可展开「我的角色与场景」（总结性占位文案，后续可由 AI/接口替换）；取消顶区固定句「练习间已就绪…」及对应 Toast；**互换**须双方均点击确认后经 `ws/room` 同步；已同步 `产品描述` 5.4、`ARCHITECTURE` 6.4 与第 11 节表、`room.html` / `room-agora.js` / `room-tasks.js` / `room-role-swap.js` / `roomTaskWs.js`。 |
| 2026-04-14 | **房间任务分页与顶区折叠**：任务区增加分页器（每次仅展示一条任务，CLAIM/HINTS 与当前条同步）；新增可折叠「场景背景」「练习提示」；已同步 `产品描述` 5.4、`ARCHITECTURE` 第 11 节表、`room.html`、`room-practice-tasks.js`、`styles.css`。 |
| 2026-04-14 | **房间顶区结构**：「场景背景」与「角色 /互换」合并为**单块** `details`折叠（内嵌角色子卡 + SWAP ROLES），去掉原独立「我的角色」卡与中间留白；已同步 `产品描述` 5.4、`ARCHITECTURE` 第 11 节房间行、`room.html`、`styles.css`。 |

---

## 5. 与 Cursor 规则的关系

项目内 `.cursor/rules/` 中的相关规则会提示：在对话中修改产品需求时，应**同步更新** `docs/产品描述.md` 与本文件的**变更记录**。  
若你希望某条规则始终生效，请保留该 `.mdc` 文件；若仅人工维护文档，也可只依赖本 Markdown 流程。
