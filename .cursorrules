# 英语口语真人匹配平台 — Cursor 规则

## 必读（优先级从高到低）
1. `docs/产品描述.md` — 产品规则与用户体验的单一事实来源。
2. `ARCHITECTURE.md` — 技术栈、目录、数据库、API、预约截止、配对与展示搭档、Cron、Agora、代码规范。
3. `docs/产品与需求变更规则.md` — 需求变更时如何留痕。

## 硬约束（摘要）
- **预约截止**：场次 `start_time` 距当前不足 **60 分钟**（服务器时区 `Asia/Shanghai`）时，**禁止新建预约**；接口返回明确 `code`/文案；列表接口标记 `can_book`。
- **配对后须告知「是谁」**：配对完成后，**通知、`GET /api/my-bookings`、`GET /api/my-pair`、个人中心**须包含**搭档昵称与口语水平**；**禁止**暴露对方手机号/邮箱。声网 **channel_name** 仅服务端生成并下发。
- **配对时机**：仍为开场前 **5 分钟**批处理生成 `pairs`（见 `ARCHITECTURE.md` Cron 窗口）。
- **前端现状**：静态页在 `backend/public/`（Express 托管）；与 `ARCHITECTURE.md` 对齐。
- **产品/业务变更**：先改 `docs/产品描述.md`，再在 `docs/产品与需求变更规则.md` 第 4 节**追加**变更记录。

实现细节一律以 `ARCHITECTURE.md` 全文为准。
