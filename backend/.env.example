# 若 3000 已被其它项目占用，可改为 3010 等，并同步修改前端 room.html 的 data-api-base
PORT=3000
# 逗号分隔的前端来源，用于 CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

# 声网控制台：项目 → App ID；并开启 App Certificate（主证书）
AGORA_APP_ID=
AGORA_APP_CERTIFICATE=
