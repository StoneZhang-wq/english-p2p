---
description: 产品或业务需求变更时同步更新 docs 下的产品描述与变更记录
alwaysApply: true
---

# 产品文档同步

本仓库为「英语口语真人匹配练习平台」。当用户**修改、新增或否定**此前已约定的产品需求、业务规则或流程时：

1. **必须**更新 `docs/产品描述.md` 中对应章节，使正文与最新决策一致。
2. 若涉及技术栈、API、库表、目录或工程约束，**同步**更新根目录 `ARCHITECTURE.md`（及必要时 `db/schema.sql`）。
3. **必须**在 `docs/产品与需求变更规则.md` 的 **「4. 变更记录」** 表格中**追加一行**（日期 + 简短摘要）。
4. 实现代码或前端时，以**已更新后的** `docs/产品描述.md` 与 `ARCHITECTURE.md` 为准，不得仅按对话口述为准。

若变更仅涉及 UI 视觉而不改变用户流程与规则，可只改代码、不改产品描述（除非用户明确要求记入文档）。
